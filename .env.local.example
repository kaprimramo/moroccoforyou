# Optional — point at the deployed Cloudflare Worker (see api/README.md).
# When unset, the AIPlanner form falls back to WhatsApp-only.
NEXT_PUBLIC_AI_API_URL=https://moroccoforyou-api.your-subdomain.workers.dev/plan-trip
